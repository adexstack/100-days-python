# day-63-advanced-databases-sqlite-sqlalchemy/main adds and show data on a website but not persistent as stopping the code run resets all.
# sqllite *default db of python) comes into the rescue by using database but overhead of sql queries, prompt to error
# sqlalchemy rescues by using exposed python functions to interact with db and also does error checking